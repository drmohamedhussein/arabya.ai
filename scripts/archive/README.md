# أرشيف سكربتات التصحيح القديمة

هذه الملفات (`patch_*.py`) **للأرشيف فقط** — لا تُشغَّل بعد إتمام مراحل الإصلاح 1–6.

قد تعيد منطقاً قديماً (مثل `TEACHER2026` أو بذور افتراضية غير آمنة).

أي تعديل مستقبلي يجب أن يكون مباشرة في `app.js` وملفات `js/` و`integrations/`.
